/**
 * <p>
 * A package that holds shared other packages with
 * <a href="http://junit.org/">JUnit</a> tests but itself is empty (as
 * there are no classes that could be tested at the {@code org} level).
 * </p>
 */
package shared.junit.org;